# Acceptance Criteria

1) `docker-compose.yml` defines `mcp-server` and `keystore` services with ports and env wiring.
2) `nucleus-stub` is removed from the repo and compose references.
3) README/docs mention MCP, Keystore, and external Nucleus env requirements.
